export async function GET(request:Request){const cms=process.env.PAYLOAD_CMS_URL?.replace(/\/$/,"");const path=new URL(request.url).searchParams.get("path");if(!cms||!path)return new Response("Not found",{status:404});try{const url=new URL(path,`${cms}/`);const cmsUrl=new URL(cms);const isPayloadMedia=url.origin===cmsUrl.origin&&url.pathname.startsWith("/api/media/file/");const isVercelBlob=url.protocol==="https:"&&url.hostname.endsWith(".public.blob.vercel-storage.com");if(!isPayloadMedia&&!isVercelBlob)return new Response("Not found",{status:404});const response=await fetch(url,{next:{revalidate:3600},signal:AbortSignal.timeout(8000)});if(!response.ok||!response.body)return new Response("Not found",{status:404});return new Response(response.body,{headers:{"Content-Type":response.headers.get("Content-Type")||"image/jpeg","Cache-Control":"public, max-age=3600, stale-while-revalidate=86400"}})}catch{return new Response("Not found",{status:404})}}
